apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: jira-mcp-pdb
  namespace: mcp-apps
spec:
  minAvailable: 1              # always keep at least 1 pod running
  selector:
    matchLabels:
      app: jira-mcp-server